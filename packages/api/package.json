{
  "name": "@muicv/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Mui简历 skill 背后的 Cloudflare Worker：PDF 渲染（Container + Puppeteer）、后续 JD 抓取等。",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "build": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "@muicv/shared": "workspace:*",
    "hono": "^4.7.10"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260103.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.54.0"
  }
}
